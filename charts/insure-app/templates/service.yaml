apiVersion: v1
kind: Service
metadata:
  name: {{ include "insure-app.fullname" . }}
  labels:
    {{- include "insure-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "insure-app.selectorLabels" . | nindent 4 }}
