apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: insure-backed
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: insurance-backend
            port:
              number: 8085
